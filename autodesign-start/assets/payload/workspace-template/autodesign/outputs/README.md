# Autodesign Outputs

Stage 06 creates canonical source-of-truth artifacts under `outputs/canonical/` when `generate-canonical.mjs` is applied with explicit approval.

No image files, Pencil files, visual reference sets, design-system outputs, prototypes, or handoff packages are generated in Stage 06.
