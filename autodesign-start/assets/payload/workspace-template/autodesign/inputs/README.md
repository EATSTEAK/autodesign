# Autodesign Inputs

Place real project source files here before running canonical generation.

Supported deterministic input formats include Markdown, text, JSON, CSV, TSV, and HTML. UX-related generation requires explicit platform selection in these files, such as `platform: web`, `target platform: mobile`, or a clear phrase like `responsive web app`.
