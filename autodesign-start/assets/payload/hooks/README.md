# Payload Hooks

Stage 08 includes no-op hook adapters only.

The bundled hooks are safe placeholders that return static metadata when imported or run in debug mode. They do not trigger canonical generation, image generation, visual reference records, Pencil operations, design-system generation, prototype generation, handoff, reports, or downstream phase behavior.
